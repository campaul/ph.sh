[tox]
project = photoshell
envlist = py33,py34,pypy3,cover

[testenv]
deps =
    -rrequirements.txt
    coverage
    pytest
    mock

[testenv:cover]
basepython = pyton3.4
deps =
    coverage
commands =
    coverage erase
    coverage run -m pytest tests
    coverage report --show-missing
